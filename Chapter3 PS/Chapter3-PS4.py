# Replace the double space from problem 3 with single spaces

string = input("Enter something: ")
#suppose we enter "Tejas is  a good boy"

print(string.replace("  ", " "))

#Strings are immutable, means you cannot change them by runnung functions on them.